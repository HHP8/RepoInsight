# Partial history fixture

This source tree is used with a bounded local bare remote in acquisition tests. The files are static; the test creates Git history outside this directory and clones it with the configured depth.
