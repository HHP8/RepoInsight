# Healthy fixture

## Overview

A small documented package used by RepoInsight end-to-end tests.

## Installation

Run `pip install .` in a disposable environment.

## Usage

```python
from healthy import add

assert add(1, 2) == 3
```

## Testing

Run `pytest`.
