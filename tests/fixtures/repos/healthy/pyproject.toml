[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "repoinsight-healthy-fixture"
version = "1.0.0"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
