# Analyzer failure fixture

This ordinary source tree is paired with an injected test analyzer that raises a synthetic exception. Repository content itself cannot and must not trigger executable analyzer code.
